PKG_CPPFLAGS = -DCOZIP_BUILDING `pkg-config --cflags libzip 2>/dev/null`
PKG_LIBS     = `pkg-config --libs libzip 2>/dev/null || echo -lzip`

OBJECTS = cozip.o cozip_glue.o

all: $(SHLIB)

$(OBJECTS): version.h

version.h:
	@echo '#define COZIP_VERSION_STRING "$(PACKAGE_VERSION)"' > $@